# Audio Files

Place your song files (e.g., .mp3, .wav) in this directory.

To use them in the app, open `src/lib/placeholder-data.ts` and update the `songs` array. Change the `songUrl` property for a song to point to your local file.

For example:
`songUrl: "/audio/your-song-filename.mp3"`
